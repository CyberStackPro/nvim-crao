-- =============================
-- Statusline setup
-- =============================
-- require("lualine").setup({
--  options = { theme = "ayu_dark" }
--})
require("lualine").setup({
  options = {
    theme = "ayu",
    section_separators = "",
    component_separators = "",
  },
})

